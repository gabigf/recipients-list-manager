# Project Instructions

@PLAN.md

- Follow PLAN.md exactly. Don't introduce dependencies or architectural choices not already in it.

# Code conventions

- Always use descriptive variable and parameter names — never single letters like `r`, `i`, or `x`, including inside `.map()`, `.filter()`, and `.find()` callbacks.
- In all Vue SFCs, order blocks as `<template>` first, then `<script setup>`, then `<style>` (if present).
